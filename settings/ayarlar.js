require("dotenv").config();

module.exports = {
    //TOKEN: process.env.TOKEN || "TOKEN GİR",  // bot token gir :)
    PREFIX: process.env.PREFIX || "!", // bot prefix
    SAHİP: process.env.SAHİP || "303825366756229130", //sahip id :()
}

